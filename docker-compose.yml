services:
  drawio:
    image: jgraph/drawio:latest
    ports: ["8081:8080"]
  next-ai-draw-io:
    build:
      context: .
      args:
        - NEXT_PUBLIC_DRAWIO_BASE_URL=http://localhost:8081
    ports: ["3000:3000"]
    env_file: .env
    depends_on: [drawio]
